# 代码库入门示例

以下是完整 13 项交付物的**示例记录**，用于演示模板字段和清单核验；其中终端记录标识仅定位本示例，并非本仓库或外部项目的真实运行事实。真实使用时，必须替换为可访问的日志路径、终端记录或 CI URL 与 job step。

## 场景一：完整 Web 仓库

**请求：**“新成员明天接手结算 Web 应用。请找启动、请求到数据库的路径、测试和风险；90 分钟内完成。”

### 1. 任务卡与阅读预算
- 决定：能否安全开始结算改动。
- 范围/非目标：Web、API、结算包、测试和 CI；不审查全部 UI 与历史迁移。
- 入口线索：根工作区清单、`apps/web/src/main.tsx`、`apps/api/src/server.ts`。
- 初始预算与停止条件：`90` 分钟；关键结算路径、运行面与测试面均有证据即停止。

### 2. 证据账本
| 状态 | 结论 | 证据 | 影响或验证动作 |
|---|---|---|---|
| [已知] | 工作区包含 Web、API 与结算共享包 | `package.json` 的 workspaces | 作为目录地图边界 |
| [推测] | API 处理器调用结算包后写库并请求支付服务 | `apps/api/` 对 `packages/billing/` 的导入 | 读处理器和存储适配器验证 |
| [未知] | 是否覆盖支付回调重放 | 未找到回调失败用例 | 读回调测试或新增回归测试 |

### 3. 仓库与目录地图
| 路径 | 责任 | 与任务关系 | 已读/排除 | 证据 |
|---|---|---|---|---|
| `apps/web/` | 浏览器入口与结算页面 | 用户触发 | 已读 | `apps/web/src/main.tsx` |
| `apps/api/` | 路由与服务组合根 | 请求入口 | 已读 | `apps/api/src/server.ts` |
| `packages/billing/` | 金额与支付业务规则 | 关键业务 | 已读 | `packages/billing/src/charge.ts` |

### 4. 入口地图
| 入口类型 | 路径/符号 | 触发方式 | 下一个边界 | 证据 |
|---|---|---|---|---|
| 浏览器 | `apps/web/src/main.tsx` | 用户提交结算 | API 请求客户端 | 文件入口 |
| API | `apps/api/src/server.ts` 的结算路由 | `POST /checkout` | `charge` 服务 | 路由注册 |
| 业务 | `packages/billing/src/charge.ts` | API 调用 | 存储与支付适配器 | 导入关系 |

### 5. 关键数据/控制流
1. 提交结算 -> Web 请求客户端 -> `POST /checkout` -> 鉴权/金额校验 -> `charge.ts` -> 订单存储与支付适配器 -> 响应。
2. 异步/失败边界：支付回调和写库事务；回调重放处理为 `[未知]`。

### 6. 配置与秘密边界
| 键名或配置面 | 来源 | 环境差异 | 是否已验证 | 安全边界 |
|---|---|---|---|---|
| `PAYMENT_PROVIDER_KEY` | `.env.example` | 本地/CI 由环境注入 | 仅键名已知 | 不读取值 |
| 数据库 URL | 容器服务定义 | 本地容器与 CI 服务不同 | `[推测]` | 不输出连接串 |

### 7. 命令证据
| 目的 | 命令 | 工作目录 | 前提 | 状态 | 执行时间 | 退出码 | 关键输出摘要（脱敏） | 可定位证据（日志/终端/CI URL + job step） | 阻断原因 |
|---|---|---|---|---|---:|---|---|---|---|
| API 测试 | `pnpm --filter api test` | 仓库根目录 | 依赖已安装 | [已运行-通过] | `2026-07-26T09:20:00+08:00` | `0` | `billing integration: 12 passed` | 示例终端记录 `web-001#1` | 不适用：已执行 |
| 本地 Web 启动 | `pnpm --filter web dev` | 仓库根目录 | 允许占用端口 | [未运行] | 未运行，不填 | 未运行，不填 | 未运行，不填 | 未运行，不填 | 预算只覆盖静态地图与 API 测试 |

### 8. 测试拓扑
| 层级 | 位置/命令 | 覆盖的任务路径 | fixture/mock/依赖 | CI/缺口 |
|---|---|---|---|---|
| 单元 | `packages/billing/**/*.test.ts` | 金额与优惠计算 | 支付适配器 mock | 未证明回调重放 |
| 集成 | `apps/api/test/checkout.*` | 路由到 `charge` | 测试数据库 | CI 的 `api-tests` job |
| 端到端 | 不适用 | 结算 UI 不在本次范围 | 不适用 | 重新纳入时再定位 |

### 9. 风险热区
| 热区 | 影响路径 | 证据 | 置信度 | 最小检查或缓解 |
|---|---|---|---|---|
| 金额与订单写入 | `charge.ts` -> 数据库 | 关键路径导入 | 中 | 读事务边界 |
| 支付回调重放 | 回调 -> 订单状态 | 缺少失败用例 | 中 | 读回调测试 |

### 10. 已排除区域
| 区域 | 不读理由 | 重新纳入条件 |
|---|---|---|
| 全部 UI 页面 | 非目标，入口已定位 | 结算页面状态影响请求契约 |
| 历史迁移 | 不支持当前决定 | 写入 schema 或迁移兼容性被阻断 |

### 11. 可作决定与阻断项
- 现在可以：从 API 路由、`charge.ts` 和集成测试开始计划结算改动。
- 仍被阻断：支付回调重放与生产环境变量来源未验证。

### 12. 下一步阅读顺序
1. `apps/api/` 的结算处理器：确认鉴权和金额校验；停止条件：找到服务调用和错误映射。
2. 存储事务与回调测试：确认重放处理；停止条件：找到幂等契约或明确其缺失。

### 13. 最小交接
- 入口与测试：`POST /checkout` -> `charge.ts`；先运行 API 集成测试。
- 不能越过的边界：不读取支付密钥或数据库连接串。
- 风险与未知：金额写入、回调重放、生产配置来源。
- 给下一位协作者的请求：补齐回调重放证据后再修改结算状态。

#### 清单核验
- 范围与阅读预算：通过，任务、非目标、入口、停止条件、排除区域和扩展条件均已写明。
- 地图与证据：通过，目录、入口、流、配置、已知/推测/未知、风险和下一步均可定位。
- 命令与秘密：通过，已运行命令具有完整执行证据；未运行命令只写计划目录和阻断原因；无秘密值。
- 测试与交接：通过，层级/缺口、13 项和最小交接均已填写。

## 场景二：无文档且无法启动

**请求：**“这个仓库没有 README，帮我启动它；如果不能启动，告诉我最少缺什么。”

### 1. 任务卡与阅读预算
- 决定：定位第一个启动阻断项。
- 范围/非目标：清单、配置样例、容器/CI 与启动入口；不修复环境或枚举全部服务。
- 入口线索：根 `package.json` 的 `dev` 脚本。
- 初始预算与停止条件：`30` 分钟；确认变量来源或需用户提供的受控环境即停止。

### 2. 证据账本
| 状态 | 结论 | 证据 | 影响或验证动作 |
|---|---|---|---|
| [已知] | 根脚本提供 `npm run dev` | `package.json` scripts | 最小启动尝试 |
| [已知] | 启动缺少 `DATABASE_URL` | 命令记录 `no-docs-001#1` | 读容器/CI |
| [未知] | 本机数据库与受控凭据是否可用 | 未提供环境资料 | 请求运行环境说明 |

### 3. 仓库与目录地图
| 路径 | 责任 | 与任务关系 | 已读/排除 | 证据 |
|---|---|---|---|---|
| `package.json` | 启动脚本 | 最小入口 | 已读 | scripts |
| `compose.yaml` | 本地依赖服务 | 变量来源 | 已读 | service env |
| `src/server.ts` | 服务启动入口 | 次级入口 | 已读 | `dev` 脚本目标 |

### 4. 入口地图
| 入口类型 | 路径/符号 | 触发方式 | 下一个边界 | 证据 |
|---|---|---|---|---|
| npm 脚本 | `package.json` 的 `dev` | `npm run dev` | `src/server.ts` | scripts |
| 服务 | `src/server.ts` | 脚本加载 | 数据库配置 | 配置读取 |

### 5. 关键数据/控制流
1. `npm run dev` -> `src/server.ts` -> 环境校验 -> 数据库客户端 -> HTTP 监听。
2. 失败边界：环境校验先于监听；缺少 `DATABASE_URL` 时停止。

### 6. 配置与秘密边界
| 键名或配置面 | 来源 | 环境差异 | 是否已验证 | 安全边界 |
|---|---|---|---|---|
| `DATABASE_URL` | `compose.yaml` 与配置样例 | 本地容器/CI 注入 | 键名和缺失已验证 | 不读取值 |
| 应用密钥 | 不适用：未读取 | 不适用 | 不适用 | 不猜测或伪造 |

### 7. 命令证据
| 目的 | 命令 | 工作目录 | 前提 | 状态 | 执行时间 | 退出码 | 关键输出摘要（脱敏） | 可定位证据（日志/终端/CI URL + job step） | 阻断原因 |
|---|---|---|---|---|---:|---|---|---|---|
| 最小启动 | `npm run dev` | 仓库根目录 | Node 依赖已安装 | [已运行-失败] | `2026-07-26T10:05:00+08:00` | `1` | `Missing required env: DATABASE_URL` | 示例终端记录 `no-docs-001#1` | 不适用：已执行 |
| 容器启动 | `docker compose up db` | 仓库根目录 | 允许启动本地容器 | [未运行] | 未运行，不填 | 未运行，不填 | 未运行，不填 | 未运行，不填 | 未确认 Docker 权限与受控数据策略 |

### 8. 测试拓扑
| 层级 | 位置/命令 | 覆盖的任务路径 | fixture/mock/依赖 | CI/缺口 |
|---|---|---|---|---|
| 单元 | 不适用：未定位测试目录 | 启动诊断不是业务变更 | 不适用 | 需在 CI 配置确认 |
| 集成 | 不适用：数据库阻断启动 | 服务到数据库 | 不适用 | 读取 CI 后再判断 |
| 端到端 | 不适用：服务未启动 | 不适用 | 不适用 | 不适用 |

### 9. 风险热区
| 热区 | 影响路径 | 证据 | 置信度 | 最小检查或缓解 |
|---|---|---|---|---|
| 环境配置漂移 | 启动 -> 数据库 | 缺少 `DATABASE_URL` | 高 | 对比容器与 CI 键名 |
| 本地容器数据 | 启动依赖服务 | Docker 权限未知 | 中 | 确认是否允许使用受控空数据库 |

### 10. 已排除区域
| 区域 | 不读理由 | 重新纳入条件 |
|---|---|---|
| 业务模块 | 首个阻断在启动配置 | 环境就绪仍出现业务启动错误 |
| 生产部署文件 | 无授权读取生产配置 | 需要确认 CI/本地差异且允许访问 |

### 11. 可作决定与阻断项
- 现在可以：请求 `DATABASE_URL` 的受控来源或本地数据库运行方式。
- 仍被阻断：Docker 权限、数据库实例、凭据来源和测试命令。

### 12. 下一步阅读顺序
1. `compose.yaml`：确认数据库服务与键名；停止条件：定位本地变量来源。
2. CI workflow：确认 job 注入方式；停止条件：找到变量名与 job step，不读取值。

### 13. 最小交接
- 入口与测试：`npm run dev` 在环境校验处失败；测试位置尚未定位。
- 不能越过的边界：不伪造 `DATABASE_URL`，不读取任何现有 `.env`。
- 风险与未知：本地数据库策略、凭据来源、测试拓扑。
- 给下一位协作者的请求：提供脱敏的变量来源或允许的本地容器方案。

#### 清单核验
- 范围与阅读预算：通过，启动诊断限定为首个阻断项，未遍历业务模块。
- 地图与证据：通过，入口、配置、失败流、排除区域、风险和下一步均有说明；测试层级明确为不适用并说明理由。
- 命令与秘密：通过，失败命令有工作目录、时间、退出码、摘要和终端定位；未运行命令未填证据且给出阻断原因；无秘密值。
- 测试与交接：通过，所有 13 项与交接请求均已填写。

## 场景三：局部功能修改

**请求：**“只改 `POST /orders` 的优惠码校验，找到该路径和最近回归测试，别做全仓库导览。”

### 1. 任务卡与阅读预算
- 决定：确定最小改动路径和回归覆盖。
- 范围/非目标：订单路由、校验器、订单服务、输入 schema 和相邻测试；不读前端、报表、部署或无关订单流。
- 入口线索：`POST /orders`。
- 初始预算与停止条件：`25` 分钟；调用链闭合并定位测试即停止，跨包契约阻断时仅扩展一跳。

### 2. 证据账本
| 状态 | 结论 | 证据 | 影响或验证动作 |
|---|---|---|---|
| [已知] | 路由调用优惠码校验器 | `src/routes/orders.ts` 的处理器导入 | 读校验器 |
| [已知] | 集成测试覆盖无效优惠码 | `test/orders.integration.ts` | 作为最近回归测试 |
| [未知] | 共享促销包是否定义过期规则 | 校验器接口未展开 | 仅在需要时读共享包 |

### 3. 仓库与目录地图
| 路径 | 责任 | 与任务关系 | 已读/排除 | 证据 |
|---|---|---|---|---|
| `src/routes/orders.ts` | HTTP 路由 | 入口 | 已读 | `POST /orders` |
| `src/promotions/validate.ts` | 优惠码校验 | 业务规则 | 已读 | 路由导入 |
| `test/orders.integration.ts` | 订单回归测试 | 验证 | 已读 | 测试名称 |

### 4. 入口地图
| 入口类型 | 路径/符号 | 触发方式 | 下一个边界 | 证据 |
|---|---|---|---|---|
| API | `src/routes/orders.ts` 的 `POST /orders` | 客户端请求 | 输入 schema | 路由注册 |
| 校验 | `validatePromotion` | 订单处理器调用 | 订单服务 | 导入关系 |
| 测试 | `test/orders.integration.ts` | API 请求 fixture | 测试数据库 | 相邻用例 |

### 5. 关键数据/控制流
1. `POST /orders` -> 输入 schema -> `validatePromotion` -> 订单服务 -> 订单写入 -> 响应。
2. 失败边界：优惠码无效、过期或订单金额变化；过期规则的共享契约为 `[未知]`。

### 6. 配置与秘密边界
| 键名或配置面 | 来源 | 环境差异 | 是否已验证 | 安全边界 |
|---|---|---|---|---|
| 测试数据库 URL | 测试配置引用 | CI/local 可能不同 | 仅测试名称已知 | 不读取值 |
| 促销开关 | 不适用：当前路径未读取 | 不适用 | 不适用 | 共享包阻断前不扩展 |

### 7. 命令证据
| 目的 | 命令 | 工作目录 | 前提 | 状态 | 执行时间 | 退出码 | 关键输出摘要（脱敏） | 可定位证据（日志/终端/CI URL + job step） | 阻断原因 |
|---|---|---|---|---|---:|---|---|---|---|
| 订单集成测试 | `pnpm --filter api test -- orders.integration` | 仓库根目录 | 测试数据库 fixture 可用 | [已运行-通过] | `2026-07-26T10:40:00+08:00` | `0` | `orders integration: 8 passed` | 示例终端记录 `orders-001#1` | 不适用：已执行 |
| 全量端到端 | `pnpm e2e` | 仓库根目录 | 浏览器与环境已配置 | [未运行] | 未运行，不填 | 未运行，不填 | 未运行，不填 | 未运行，不填 | 局部校验改动不需要全量浏览器覆盖，除非路由契约变更 |

### 8. 测试拓扑
| 层级 | 位置/命令 | 覆盖的任务路径 | fixture/mock/依赖 | CI/缺口 |
|---|---|---|---|---|
| 单元 | `src/promotions/validate.test.ts` | 校验边界 | 固定时间/优惠 fixture | 需确认过期规则 |
| 集成 | `test/orders.integration.ts` | 路由到写入 | 测试数据库 | 订单 CI job |
| 端到端 | 不适用：未改 UI 契约 | 不适用 | 不适用 | 路由响应变化才纳入 |

### 9. 风险热区
| 热区 | 影响路径 | 证据 | 置信度 | 最小检查或缓解 |
|---|---|---|---|---|
| 优惠过期 | 校验 -> 订单金额 | 共享规则未读 | 中 | 读共享促销契约 |
| 总额写入 | 订单服务 -> 数据库 | 路由流程 | 中 | 运行相邻失败用例 |

### 10. 已排除区域
| 区域 | 不读理由 | 重新纳入条件 |
|---|---|---|
| 前端结算页面 | 请求/响应契约未变 | API 错误响应或字段变化 |
| 报表与部署 | 无关局部规则 | 发布/回滚或数据聚合被影响 |
| 共享促销包 | 尚未阻断调用链 | 当前校验器需要其过期规则 |

### 11. 可作决定与阻断项
- 现在可以：在校验器与订单集成测试中实现并验证局部规则。
- 仍被阻断：过期优惠是否由共享促销包统一定义。

### 12. 下一步阅读顺序
1. `src/promotions/validate.ts`：确认边界与错误映射；停止条件：得到输入/输出契约。
2. 共享促销包的过期规则：仅当校验器委托该规则时读取；停止条件：确认所有权或决定不扩展。

### 13. 最小交接
- 入口与测试：`POST /orders` -> `validatePromotion`；先运行订单集成测试。
- 不能越过的边界：不改前端、报表、部署或共享包，除非过期契约阻断。
- 风险与未知：过期优惠规则与总额写入。
- 给下一位协作者的请求：先确认共享过期契约，再提交最小校验改动和相邻回归测试。

#### 清单核验
- 范围与阅读预算：通过，局部调用链、非目标、排除区域和一跳扩展条件均明确。
- 地图与证据：通过，目录、入口、流、配置、证据状态、风险、决定和阅读顺序齐全。
- 命令与秘密：通过，已运行测试有完整可定位证据；未运行端到端没有填造证据并说明不运行理由；无秘密值。
- 测试与交接：通过，单元/集成/端到端适用性、缺口、13 项和最小交接均已填写。

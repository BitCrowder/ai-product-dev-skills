# Codebase Onboarding Example

```text
Use $codebase-onboarding to orient me in this repo before I implement a billing feature.
```

```markdown
Start here: `src/app.ts` wires routes, `src/billing/` owns subscription logic, and `tests/billing/` has the closest regression coverage.
```

# 代码库入门模板

```markdown
# 代码库入门简报：<任务>

## 1. 任务卡与阅读预算
- 决定：
- 范围/非目标：
- 入口线索：
- 初始预算与停止条件：

## 2. 证据账本
| 状态 | 结论 | 证据（路径/命令/材料） | 影响或验证动作 |
|---|---|---|---|
| [已知] | | | |
| [推测] | | | |
| [未知] | | | |

## 3. 仓库与目录地图
| 路径 | 责任 | 与任务关系 | 已读/排除 | 证据 |
|---|---|---|---|---|

## 4. 入口地图
| 入口类型 | 路径/符号 | 触发方式 | 下一个边界 | 证据 |
|---|---|---|---|---|

## 5. 关键数据/控制流
1. <触发> -> <入口> -> <校验/鉴权> -> <业务模块> -> <存储/外部服务> -> <响应/副作用>
2. 失败/异步边界：

## 6. 配置与秘密边界
| 键名或配置面 | 来源 | 环境差异 | 是否已验证 | 安全边界 |
|---|---|---|---|---|

## 7. 命令证据
| 目的 | 命令 | 工作目录 | 前提 | 状态 | 执行时间 | 退出码 | 关键输出摘要（脱敏） | 可定位证据（日志/终端/CI URL + job step） | 阻断原因 |
|---|---|---|---|---|---|---:|---|---|---|
| 启动 | | `<计划工作目录>` | | [未运行] | 未运行，不填 | 未运行，不填 | 未运行，不填 | 未运行，不填 | `<未运行的阻断原因>` |
| 测试 | | `<计划工作目录>` | | [未运行] | 未运行，不填 | 未运行，不填 | 未运行，不填 | 未运行，不填 | `<未运行的阻断原因>` |

## 8. 测试拓扑
| 层级 | 位置/命令 | 覆盖的任务路径 | fixture/mock/依赖 | CI/缺口 |
|---|---|---|---|---|

## 9. 风险热区
| 热区 | 影响路径 | 证据 | 置信度 | 最小检查或缓解 |
|---|---|---|---|---|

## 10. 已排除区域
| 区域 | 不读理由 | 重新纳入条件 |
|---|---|---|

## 11. 可作决定与阻断项
- 现在可以：
- 仍被阻断：

## 12. 下一步阅读顺序
1. <路径或命令>：<减少哪项未知>；停止条件：<何时停止>。
2. ...

## 13. 最小交接
- 入口与测试：
- 不能越过的边界：
- 风险与未知：
- 给下一位协作者的请求：
```

状态只能取 `[已知]`、`[推测]`、`[未知]`；命令状态只能取 `[已运行-通过]`、`[已运行-失败]`、`[未运行]`。`[已运行-通过]` 与 `[已运行-失败]` 必填工作目录、执行时间、退出码、关键输出摘要和可定位证据；没有阻断原因时填“不适用：已执行”。`[未运行]` 只填计划工作目录和阻断原因，执行时间、退出码、关键输出摘要和可定位证据一律为“未运行，不填”。不要在模板中填写秘密值或未脱敏日志。

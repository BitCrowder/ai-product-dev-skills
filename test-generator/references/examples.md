# Test Generator Example

```text
Use $test-generator to add tests for CSV import validation.
```

```markdown
Cases: valid file, missing required column, malformed row, duplicate email, empty file, large file.
```
